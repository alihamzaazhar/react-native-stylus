import React,{useState} from 'react'; import {Text} from 'react-native'; import {StylusCanvas,type StylusInputEvent} from 'react-native-stylus'; import {Card,LabScreen} from '../components/Lab'; import {styles} from '../styles';
export function HoverScreen(){const [event,setEvent]=useState<StylusInputEvent>(); return <LabScreen eyebrow="HOVER PREVIEW" title="Cursor, distance and focus"><Text style={styles.body}>Move the nib above the surface. The ring previews pressure/tilt-adjusted brush size.</Text><StylusCanvas style={styles.canvas} hoverEnabled brushPreviewEnabled pointerIcon="crosshair" strokeWidth={18} onStylusEvent={setEvent}/><Card><Text style={styles.metric}>action={event?.action??'waiting'} distance={event?.point.distance.toFixed(4)??'-'} tilt={event?.point.tilt.toFixed(4)??'-'}</Text></Card></LabScreen>;}
