import React,{useState} from 'react'; import {Text} from 'react-native'; import {Stylus,StylusHandwritingInput} from 'react-native-stylus'; import {Action,Card,LabScreen} from '../components/Lab'; import {styles} from '../styles';
export function HandwritingScreen(){const [text,setText]=useState(''); const [status,setStatus]=useState({supported:false,focused:false}); return <LabScreen eyebrow="ANDROID 14+ · IME" title="Write directly into text"><Text style={styles.body}>Use a compatible handwriting IME. The native EditText supports bounds and handwriting delegation.</Text><StylusHandwritingInput style={styles.input} value={text} onChangeText={setText} onHandwritingStatus={setStatus} placeholder="Write here with a stylus" multiline autoHandwritingEnabled handwritingBounds={{left:20,top:40,right:20,bottom:40}}/><Card><Text selectable>{text||'No recognized text yet'}</Text><Text style={styles.metric}>supported={String(status.supported)} focused={String(status.focused)}</Text></Card><Action label="Choose input method" onPress={()=>Stylus.showInputMethodPicker()}/></LabScreen>;}
