import React,{useState} from 'react'; import {Text} from 'react-native'; import {Stylus} from 'react-native-stylus'; import {Action,Card,LabScreen} from '../components/Lab'; import {styles} from '../styles';
export function ImmersiveScreen(){const [enabled,setEnabled]=useState(false); const toggle=()=>{const next=!enabled;Stylus.setImmersiveMode(next).then(ok=>{if(ok)setEnabled(next);}).catch(()=>undefined);}; return <LabScreen eyebrow="FULLSCREEN + ADAPTIVE" title="Protect the drawing workspace"><Text style={styles.body}>Transient system bars reduce accidental edge interactions. Swipe from an edge to reveal bars while immersive mode is active.</Text><Action label={enabled?'Exit immersive mode':'Enter immersive mode'} onPress={toggle}/><Card><Text style={styles.metric}>immersive={String(enabled)}</Text><Text style={styles.body}>Use the capability screen to verify large-screen and ChromeOS detection.</Text></Card></LabScreen>;}
